import { FilmListFilterPipe } from './film-list-filter.pipe';

describe('FilmListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilmListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
